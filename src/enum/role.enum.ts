export enum ENUM_Role {
  Owner = 1,
  Developer = 2,
  PM = 3,
  VIEWER = 4,
  // Add more roles as needed
}

export enum ENUM_RoleName {
  Owner = 'Owner',
  Developer = 'Developer',
  PM = 'PM',
  VIEWER = 'VIEWER',
  // Add more roles as needed
}
